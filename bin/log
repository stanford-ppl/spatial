#!pir/env/bin/python3

import sys
sys.path.insert(0, 'pir/bin')
import logger

def main():
    cmd = "--logdir=pir/logs/"
    cmd += " --spatial_dir=./"
    cmd += " --pir_dir=./pir"
    args = sys.argv[1:] + (cmd.split(" "))
    logger.main(args)

if __name__ == "__main__":
    main()
