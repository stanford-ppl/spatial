package pcc.traversal.transform

import pcc.core._
import pcc.data._
import pcc.lang._
import pcc.node._

/*case class AccelInserter(IR: State) extends MutateTransformer {

  override def transform[A:Sym](lhs: Sym[A], rhs: Op[A])(implicit ctx: SrcCtx): Sym[A] = {

  }
}*/
